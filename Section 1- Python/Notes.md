BEST PLATFORMS 
GitHub copilot 
https://github.com/features/copilot
-vscode extension - helps in code completion and automate programming tools

cursor ai
https://www.cursor.com/
-free ai agent than can write and edit a code 

google collab
https://colab.research.google.com/
- gives free tbu and compute(all heavy program will run on a remote pc not on ur pc)

jupyter notebook
https://jupyter.org/ 
- can run block of codes individually - ease of use 

Anaconda - pre installed packages like numpy, pandas, matplotlib, seaborn, plotly - ease of use - can change the Pyhton env and version acc to requirements + 300 curated DS packages
- we can control python version and package version using anaconda 


Tools
Conda 
A utility tools  for managing and deploying appl, env and packages (mostly package manager)
can be used using anaconda prompt   

Anaconda navigator 
A GUI -helps in launching diff apps

miniconda - mini version on anaconda
minimal - no nummpy,pandas..etc

How to add conda to env variable(terminal)
Excute command "conda init" in anaconda prompt


.ipynb is an extension of jupyter notebook(i.e i-python notebook - its a command shell for interactive computing) used in jupyter notebook

ctrl + entre = to run the code in jupyternotebook
shift + Enter = to run the code in jupyternotebook and move to next line

NOTE: to use jupyternotebook the  notebook server should be running (anaconda prompt- local host--)

ctrl + C stops the running server 
leads to dead kernel therefore cant use jupyternotebook


# OOPS
fn_name(self)  will always be a instance function doesn't need a decorator and can access both class and instance attributes 

# HW 
- Make a class railay form
- add train number, platform number , time , static method - time and more

- #Tuple comprehension is not possible as tuple is immutable once created